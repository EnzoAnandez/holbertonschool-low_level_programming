#include <stdio.h>
#include "main.h"

/**
 * main - jaj
 * pusheando
 * Return: On success 1.
 */

int main(void)
{
	char n[] = "_putchar\n";
	int i;
for (i = 0 ; n[i] != '\0'; i++)
	_putchar(n[i]);
return (0);
}
