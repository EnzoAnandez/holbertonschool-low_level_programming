#include "main.h"

/**
 * print_alphabet - jaj
 * pusheando
 * Return: bicho
 */

void print_alphabet(void)
{
	char n[] = "abcdefghijklmnopqrstuvwxyz\n";
	int i;
for (i = 0 ; n[i] != '\0'; i++)
	_putchar(n[i]);
}
