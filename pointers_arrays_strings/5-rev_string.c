#include "main.h"

/**
 * rev_string - function that reverses a string.
 * @s: Variable pointer.
 */

void rev_string(char *s)
{
	int n;

	for (n = 0; s[n] != '\0'; n++)
	{
		_putchar (s);
	}
	for (n = )
}
