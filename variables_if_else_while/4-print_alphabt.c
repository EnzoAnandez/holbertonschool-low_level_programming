#include <stdio.h>

/**
* main - jeje
* pusheando
* Return:vuelta
*/

int main(void)
{

char n = 'a';
for (n = 'a' ; n <= 'z'; n++)
if (n != 'q' && n != 'e')
putchar(n);

putchar('\n');

return (0);
}
