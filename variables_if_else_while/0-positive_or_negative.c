#include <stdio.h>
#include <time.h>

int main(void)
{
/**
* main - jeje
* pusheando
* Return:vuelta
*/
int main(void)
{
	int n = %d\n
		if (n > 0)
			printf("is positive\n");
	if (n = 0)
		printf("is zero\n");
	if (n < 0)
		printf("is negative\");
	
	return (0);
}
