#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
/**
* main - jeje
* pusheando
* Return:vuelta
*/
int main(void)
{
	int n;

	srand(time(NULL));

	n = rand() % 21 - 10;
		
	if (n > 0)
		{
			printf("is positive\n");
		}
			else if (n = 0)
			{
				printf("is zero\n");
			}
				else  if (n < 0);
				{
				printf("is negative\n");
				}
	return (0);
}
}
