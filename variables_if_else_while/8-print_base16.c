#include <stdio.h>

/**
* main - jeje
* pusheando
* Return:vuelta
*/

int main(void)
{

char n = '0';
char e = 'a';
for (n = '0' ; n <= '9'; n++)
	putchar(n);

for (e = 'a' ; e <= 'f'; e++)
	putchar(e);

putchar('\n');

return (0);

}
